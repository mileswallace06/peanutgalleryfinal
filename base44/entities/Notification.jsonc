{
  "name": "Notification",
  "type": "object",
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  },
  "properties": {
    "user_email": {
      "type": "string",
      "description": "Recipient user email"
    },
    "type": {
      "type": "string",
      "enum": [
        "purchase_confirmed",
        "tickets_sent",
        "transfer_verified",
        "transfer_rejected",
        "buyer_confirmed",
        "sale_complete",
        "payout_processing",
        "dispute_opened",
        "dispute_resolved",
        "donation_won",
        "donation_accepted",
        "donation_expired",
        "listing_hidden",
        "listing_approved",
        "listing_rejected",
        "listing_expired",
        "sale_created",
        "ai_verified",
        "ai_rejected",
        "admin_message"
      ]
    },
    "title": {
      "type": "string"
    },
    "body": {
      "type": "string"
    },
    "read": {
      "type": "boolean",
      "default": false
    },
    "reference_id": {
      "type": "string",
      "description": "ID of related entity (purchase, listing, donation, etc.)"
    },
    "reference_type": {
      "type": "string",
      "enum": [
        "purchase",
        "listing",
        "donation",
        "event",
        "dispute"
      ]
    },
    "action_url": {
      "type": "string",
      "description": "Deep link path (e.g. /purchase/abc)"
    },
    "icon": {
      "type": "string",
      "description": "Emoji icon for the notification"
    },
    "idempotency_key": {
      "type": "string",
      "description": "Deterministic logical key (e.g. sale_created:<purchase_id>) used to group concurrent duplicate notifications and select the single canonical record for dispatch. Records sharing a key are deduplicated to one sender."
    },
    "dispatch_status": {
      "type": "string",
      "enum": [
        "pending",
        "dispatching",
        "dispatched",
        "superseded"
      ],
      "default": "pending",
      "description": "Cross-channel dispatch lifecycle. pending = enqueued, NOT yet sent (delivery withheld). dispatching = canonical record claimed for send (best-effort, non-atomic). dispatched = push/email attempted. superseded = concurrent duplicate that must NEVER dispatch; hidden from the user inbox."
    }
  },
  "required": [
    "user_email",
    "type",
    "title"
  ]
}