/**
 * Single source of truth for listing type classification.
 * Used across checkout, purchase, cards, feeds, and admin panels.
 */
export const UPGRADE_LISTING_TYPES = ['live_upgrade', 'venue_upgrade'];
export const TICKET_LISTING_TYPES = ['resale_ticket', 'venue_ticket'];